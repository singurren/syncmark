# 2026-04-29 Step 05: PBS Job 7773955 Check

## Purpose / 目的

Check whether the second real SyncMark-on-BiMark smoke job failed, and determine whether any issue is in Katana scheduling, GPU compatibility, the real generation pipeline, or the detection output quality.

检查第二次真实 SyncMark-on-BiMark smoke 作业是否失败，并判断问题属于 Katana 调度、GPU 兼容性、真实生成管线，还是检测输出质量。

## What Was Done / 完成内容

- Located the PBS output file for job `7773955`.
- Moved the raw PBS output to `results/logs/2026-04-29_pbs_7773955_syncmark_bimark_real_smoke.out`.
- Checked the assigned GPU, PyTorch CUDA visibility, generated output directory, and detection CSV.
- Summarized decoder metrics from `output_dump/bimark_c4_gpt2_2026-04-28_18-59-26/detect_result_wm.csv`.
- Checked the source of the non-fatal NLTK warning.

- 找到了作业 `7773955` 的 PBS 输出文件。
- 已将原始 PBS 输出移动到 `results/logs/2026-04-29_pbs_7773955_syncmark_bimark_real_smoke.out`。
- 检查了分配到的 GPU、PyTorch CUDA 可见性、生成输出目录和检测 CSV。
- 汇总了 `output_dump/bimark_c4_gpt2_2026-04-28_18-59-26/detect_result_wm.csv` 中的 decoder 指标。
- 检查了非致命 NLTK 警告的来源。

## Result / 结果

The PBS job completed successfully with exit status `0`. It was assigned an L40S GPU and passed the CUDA compatibility preflight:

PBS 作业以 exit status `0` 成功完成。作业分配到 L40S GPU，并通过 CUDA 兼容性预检查：

```text
PyTorch    : 2.11.0+cu130 | CUDA build: 13.0 | CUDA visible: True
Assigned GPU: NVIDIA L40S | compute capability: 8.9
Job execution was successful. Exit Status 0.
```

The generation and detection pipeline produced:

生成与检测管线产出如下：

```text
output_dump/bimark_c4_gpt2_2026-04-28_18-59-26/generation_text.jsonl: 20 lines
output_dump/bimark_c4_gpt2_2026-04-28_18-59-26/detect_result_wm.csv: 152 data rows
Detection processed successfully: 19 items
Detection skipped: 1 item with empty text/bits
```

The decoder summary over the generated, unattacked text was:

在未攻击的生成文本上，decoder 汇总结果为：

```text
syncmark_position_vote: n=76, mean_hit=0.7706, mean_ber=0.2294, exact_rate=0.1711
syncmark_alignment:     n=76, mean_hit=0.6390, mean_ber=0.3610, exact_rate=0.0263
```

At the longest checked prefix length:

在最长检查前缀长度上：

```text
length=100
syncmark_position_vote: n=19, hit=0.928, ber=0.072, exact=0.474
syncmark_alignment:     n=19, hit=0.849, ber=0.151, exact=0.105
```

There was a non-fatal NLTK warning:

出现了一个非致命 NLTK 警告：

```text
[nltk_data] Error loading punkt: certificate verify failed
```

This came from `bimark/dipper.py`, which calls `nltk.download('punkt')` at import time. The warning did not stop this run.

该警告来自 `bimark/dipper.py`，其中在 import 时调用了 `nltk.download('punkt')`。该警告没有中断本次运行。

## What Was Achieved / 达成内容

The corrected GPU resource strategy was validated: requesting `gpu_model=L40S` avoided the V100 compute capability failure seen in job `7773895`.

修正后的 GPU 资源策略已被验证：申请 `gpu_model=L40S` 避免了作业 `7773895` 中出现的 V100 compute capability 不兼容问题。

The real SyncMark-on-BiMark generation and detection plumbing also ran end-to-end on Katana.

真实 SyncMark-on-BiMark 的生成与检测管线已经在 Katana 上端到端跑通。

## Meaning / 结果含义

This job did not fail at the cluster or CUDA level. The remaining issue is experimental: on unattacked text, the alignment decoder is worse than direct position vote. This is not surprising, because the current job did not apply insertion/deletion drift, and position vote should be strongest when there is no synchronization loss.

这次作业并没有在集群或 CUDA 层面失败。剩下的问题是实验设计层面的：在未攻击文本上，alignment decoder 反而弱于直接 position vote。这并不意外，因为当前作业没有施加 insertion/deletion drift；在没有同步丢失时，position vote 本来就应该更强。

The run validates plumbing, not the thesis claim. The thesis-relevant smoke test still needs an explicit drift attack stage before detection.

本次运行验证的是管线，不是 thesis claim。与 thesis 相关的 smoke test 仍需要在检测前加入明确的 drift attack 阶段。

## Next Step / 下一步

Add a small drift-attack smoke stage for the generated text, then compare `syncmark_position_vote` and `syncmark_alignment` after deletion/insertion attacks. Also remove or guard the import-time `nltk.download('punkt')` path before larger runs, because cluster jobs should not depend on runtime NLTK downloads.

下一步应为生成文本加入一个小规模 drift attack smoke 阶段，然后在 deletion/insertion 攻击后比较 `syncmark_position_vote` 与 `syncmark_alignment`。同时，在大规模实验前需要移除或保护 import-time 的 `nltk.download('punkt')` 路径，因为集群作业不应依赖运行时 NLTK 下载。
